{
  "version": 2,
  "buildCommand": "cd public && npm install && npm run build",
  "outputDirectory": "public/dist",
  "builds": [
    {
      "src": "src/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/server.js"
    }
  ]
}
